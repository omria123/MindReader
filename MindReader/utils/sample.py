class Sample:
	def __init__(self, user, snapshots, fields):
		self.user = user
		self.snapshots = snapshots
		self.fields = fields


class User:  # empty template which is assigned dynamically
	pass


class Snapshot:  # empty template which is assigned dynamically
	pass
